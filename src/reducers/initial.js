/**
 * Reducers specify how the application's state changes in response to actions
 * sent to the store. Remember that actions only describe what happened, but
 * don't describe how the application's state changes.
 * @type {{}}
 */

// will have an initial state
const initialState = {}

/**
 * Based on the action type, we will return a new state object with our
 * modification.
 * @type {{}}
 */
export default (state = initialState, action) => {
  return state
}
